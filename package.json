{
  "name": "luna-dating",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "scripts": {
    "build": "mkdir -p public && cp App.tsx index.html index.tsx metadata.json package.json types.ts vercel.json public/ && cp -r components services utils public/"
  }
}
